package application;

import java.net.URL;
import java.util.ResourceBundle;
import javafx.fxml.FXML;
import javafx.scene.control.Button;
import javafx.scene.control.TextArea;

public class Car_premium {

    @FXML
    private ResourceBundle resources;

    @FXML
    private URL location;

    @FXML
    private TextArea infowind;

    @FXML
    private Button inf;

    @FXML
    private Button Merc;

    @FXML
    private Button bmw;

    @FXML
    private Button client_choose_car;

    @FXML
    void initialize() {
        assert infowind != null : "fx:id=\"infowind\" was not injected: check your FXML file 'Client_car(premium).fxml'.";
        assert inf != null : "fx:id=\"inf\" was not injected: check your FXML file 'Client_car(premium).fxml'.";
        assert Merc != null : "fx:id=\"Merc\" was not injected: check your FXML file 'Client_car(premium).fxml'.";
        assert bmw != null : "fx:id=\"bmw\" was not injected: check your FXML file 'Client_car(premium).fxml'.";
        assert client_choose_car != null : "fx:id=\"client_choose_car\" was not injected: check your FXML file 'Client_car(premium).fxml'.";

    }
}
